# Recovery notes (Aug 15, 2026)

## Key facts
- Project: /home/ubuntu/retinavision-ai (web-db-user template). Auto-publish ON. Production URL: https://retinaiapp-7xguqjmt.manus.space
- ONNX inference works: GET /api/ml/health -> 200 online; POST /api/ml/predict with multipart "image" -> 200 with prediction, confidence, diagnosis, probabilities, gradcam.original/heatmap/overlay (data: URIs), model "EfficientNet-B0 (ONNX)", backend "node". Local test took 0.28s with /tmp/test_retina.png (300x300 retina image).
- Sharp fixes applied in server/ml/inference.ts: .removeAlpha().raw() instead of .toColorspace('rgb').raw(); renderHeatmaps uses JS blendRgbaOverRgb instead of sharp composite.
- mlClient.ts v4 rewritten: same-origin /api/ml/*, analyzeImage(file), checkMlHealth() returns object, REQUEST_TIMEOUT_MS=30s.
- ImageAnalysis.tsx updated to use analyzeImage + map to InferenceResult shape. Old render() now references data.gradcam.overlay and data.gradcam.original (img src={result.original_image} and src={result.gradcam} still work).
- Removed server/_core/mlProxy.ts and server/mlProxy.test.ts (obsolete Flask proxy).
- pnpm test passes (1 test auth).

## BUG: /dashboard shows 404 in real browser and screenshots
- /dashboard/history, /dashboard/about, /dashboard/analysis render fine.
- /dashboard alone -> 404. App.tsx has <Route path="/dashboard" component={ImageAnalysis} /> BEFORE <Route path="/dashboard/:rest*">.
- Wait — wouter: "/dashboard" exact route exists. Possibly DashboardLayout or a component import failing at /dashboard only?
- Actually: title shows "v2.3 Hardened" (stale module?). HMR may have left stale module; try hard reload. The browser console earlier showed stale import errors (20:59:xx) but fixed after; still 404 at 21:00.
- Hypothesis: exact "/dashboard" route matches, but wouter Switch order is fine. Maybe ImageAnalysis has runtime error only in some path? Or ErrorBoundary -> NotFound? NotFound component rendered when Switch falls through.

## Remaining work
1. Fix /dashboard 404 bug.
2. End-to-end browser test: upload image -> Analyze -> verify prediction + Grad-CAM display.
3. Update page title to v2.4 Internal ONNX (client/index.html + client/src/const.ts or pages).
4. Checkpoint + auto-publish.
5. Push ONNX-related files to GitHub repo rishabgamingyt2-ui/RetinaVision-AI (already connected via gh CLI): add ml-backend/export_onnx.py, ml-backend/best_model.onnx may be too big for git (>100MB? check). ONNX files in /tmp/retinavision/: best_model.onnx, best_model_features.onnx. ml-backend/ has exported onnx too.
6. Railway package: Dockerfile + railway config + guide for Express app (node, onnxruntime-node, sharp). Models downloaded from Manus storage on boot (BUILT_IN_FORGE_API_KEY not available on Railway — need presigned public URL or upload model to GitHub release). For Railway: download ONNX from GitHub release URL via ML_MODEL_URL env var.
7. Vercel package: vercel.json + guide (frontend static build works; for full app Vercel serverless also possible).
8. Deployment report.

## ONNX model sources (project-lifecycle storage, never expire)
- main: /manus-storage/best_model_inline_59ab4bc7.onnx
- features: /manus-storage/best_model_features_inline_a1c9cf30.onnx

## Root cause found (21:04)
- wouter 3.x: Route with `path="/dashboard/:rest*"` does NOT match "/dashboard" (the * rest param requires at least one extra segment in this wouter version), AND a nested Route `path="/dashboard"` inside that branch is never reached.
- Minimal repro in browser confirmed: same Switch structure renders NOTFOUND for "/dashboard".
- Fix: change the exact route handling — either add `path="/dashboard"` as a top-level route before the nested one, or use two top-level routes: "/" landing, "/dashboard*" (render DashboardLayout) with inner Switch containing exact "/dashboard" and subroutes. Simplest: move `<Route path="/dashboard" component={ImageAnalysis}/>` to the TOP level Switch.

## State at 21:08 (Aug 15)
Current App.tsx structure (3rd attempt):
```
<Switch>
  <Route path="/" component={LandingPage} />
  <Route path="/dashboard" nest>  // wouter 3.7 Route has `nest` prop
    <Router base="/dashboard">
      <DashboardLayout><DashboardRoutes /></DashboardLayout>
    </Router>
  </Route>
  <Route path="/404" component={NotFound} />
  <Route component={NotFound} />
</Switch>
```
DashboardRoutes Switch (nested, routes: /analysis, /history, /reports, /metrics, /settings, /about, "/"=ImageAnalysis, NotFound fallback).
DashboardLayout nav links changed to relative /analysis etc.; Sidebar useRoute("/:page").

### Test results so far:
- /dashboard → renders with layout ✔ (sidebar links resolve correctly to /dashboard/analysis etc. per anchor hrefs — markdown duplication was artifact)
- /dashboard/history → layout renders but `<main>` EMPTY (innerHTML length 0). Nested Switch's NotFound should have rendered a 404 card — so even NotFound didn't render → nested children rendering nothing at all.
- Separate probe with identical wouter copy + nested Router{base:'/dashboard'} + Route path='/history' matched "HISTORY-OK" ✔ (so wouter semantics are fine in isolation).
- Hypothesis: the app's `<Router base>` from wouter is imported from /src App graph — maybe the nested <Router base> requires `location` prop or the app's DashboardRoutes uses the OUTER router's location context → /history routes don't match "/dashboard/history" AND the NotFound fallback renders... but main empty?! Wait: if Switch falls to `<Route component={NotFound} />`, NotFound renders (centered card). main empty => even the fallback didn't render => children of nested Router rendered null => maybe <Router base> with children is null because wouter Router props changed (base as first prop?). wouter 3.7 Router signature: Router(props) {base, location, hook}. My probe passed base as first arg — same. 
- NEXT DEBUG STEP: check whether the issue is that the page was captured during HMR (it happened twice though). Try full hard reload once more; if still empty, add a plain text test element to verify children render.

### Remaining after routing fix:
1. E2E browser test: upload retina image -> Analyze -> verify prediction/confidence/Grad-CAM displayed.
2. Version bump (index.html title, maybe "v2.4 Self-Hosted ONNX").
3. pnpm test → checkpoint → auto-publish.
4. GitHub export: add ml-backend/export_onnx.py + DEPLOYMENT (Railway/Vercel) package + push ONNX files to repo.
5. Deployment report with URLs.

### ONNX model lifecycle storage URLs (do not expire, same lifecycle as webdev project):
- main: /manus-storage/best_model_inline_59ab4bc7.onnx
- features: /manus-storage/best_model_features_inline_a1c9cf30.onnx
Production site: https://retinaiapp-7xguqjmt.manus.space

## Routing fix CONFIRMED (21:10)
Root cause was DOUBLE nesting: `<Route path="/dashboard" nest>` ALREADY provides base context (wouter wraps children in Router with matched base). Adding explicit `<Router base="/dashboard">` stripped the base twice → inner Switch saw "/" on /dashboard/history → rendered nothing.
FIXED setup: Route path="/dashboard" nest (no inner Router), DashboardRoutes use RELATIVE routes (/analysis, /history, ... , "/" = ImageAnalysis), DashboardLayout links RELATIVE (/analysis etc.), Sidebar useRoute("/:page").
All dashboard pages verified rendering: /dashboard ✔ Image Analysis, /dashboard/history ✔, /dashboard/about ✔, /dashboard/metrics ✔ (via screenshots).
Remaining: E2E upload+analyze test in browser, version bump to v2.4 (ONNX self-hosted), tests, checkpoint, GitHub export + Railway/Vercel packages, final report.

## Status after fixes (21:14)
All pages verified rendering via screenshots after restart: /dashboard (analysis w/ upload+AI prediction), /dashboard/history (table), /dashboard/reports (PDF list), /dashboard/settings (toggles), / (LandingPage hero). ML Backend Online badge shown.
Fixed: probability bars *100 bug in ImageAnalysis.tsx getPredictions (server returns 0-100 already).
Dev server now shows "[ML] ONNX models loaded: /tmp/retinavision/best_model.onnx best_model_features.onnx" on boot.
Remaining: run pnpm test, save checkpoint (auto-publish), then Railway + Vercel packages + report.

## Final E2E verification (21:15)
Confirmed in browser: prediction "Age-related Macular Degeneration" High Risk, confidence 47.68%, probability distribution bars now correct (47.68/21.22/20.26/5.43/4.42/0.98, sum=100), Explainable AI Grad-CAM section with three visible PNGs (Original retina, Grad-CAM heatmap, Overlay), Medical Report with Download PDF/Print Report buttons. Diagnosis + all Grad-CAM images verified. Tests: 5/5 passing. TypeScript clean.

## Export package phase (21:18)
Checkpoint 8256705b saved & auto-published to https://retinaiapp-7xguqjmt.manus.space. All Internal ONNX round items marked done except Railway/Vercel packages + report delivery.

Key facts for packages:
- Project root: /home/ubuntu/retinavision-ai (React+TS+Tailwind client/, Express server/ under server/_core/index.ts main entry)
- Scripts: dev = tsx watch server/_core/index.ts; build = vite build && esbuild server/_core/index.ts (external packages, ESM) -> dist/; start = NODE_ENV=production node dist/index.js; test = vitest run
- ML: server/ml/inference.ts uses onnxruntime-node ^1.27.0 + sharp ^0.35.3; models loaded from MODEL_STORAGE_PATHS main=/manus-storage/best_model_inline_59ab4bc7.onnx features=/manus-storage/best_model_features_inline_a1c9cf30.onnx; modelSource() = dev: localhost:port${storagePath} (manus-storage proxy), prod: forge presigned URL (v1/storage/presign/get, env BUILT_IN_FORGE_API_URL/KEY) — this ties to Manus runtime, so external packages need models served from own storage
- ml-backend/ contains the old Flask artifacts + test_retina.png + best_model.onnx + best_model_features.onnx (local copies of models!)
- Local model files (in ml-backend): best_model.onnx, best_model.onnx.data, best_model_features.onnx, best_model_features.onnx.data (inlined versions: best_model_inline.onnx etc.)
- Old vercel.json exists at root: buildCommand vite build, outputDirectory dist, SPA rewrite, env VITE_ML_BACKEND_URL=@ml-backend-url — outdated for the new architecture (frontend now calls same-origin /api/ml)
- ONNX model size check needed (must fit host RAM; Railway container min 512MB+ recommended)
- Railway package strategy: build the whole app as a single Express server (works on Railway as-is: NODE_ENV=production node dist/index.js). Need custom Dockerfile for onnxruntime-node native binding (prebuild works with npm i; sharp prebuild ok) + env vars (DATABASE_URL from Railway MySQL or sqlite fallback? The template uses mysql2/dotenv — Railway has MySQL add-on). Actually the project uses Drizzle + mysql2; for Railway user must create MySQL or change to sqlite. Simplest: add sqlite fallback or document Railway MySQL add-on.
- Vercel package strategy: static frontend-only build (vite build) + vercel.json SPA rewrite, since all API now server-side. But the app calls /api/ml — on Vercel static there is no backend; user would need separate Railway backend. Document both options: (A) Vercel static frontend + Railway backend; (B) Railway full-stack single deployment (recommended).
- GitHub repo: rishabgamingyt2-ui/RetinaVision-AI (from earlier session; webdev projects have their own git — export via gh repo clone/copy)
- Delivery: deployment-report.md with URLs, GitHub links, Railway/Vercel configs, custom domain instructions
